gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks

# Firebase configuration — per-environment, fetched from the team secret
# store at build time. Contains the FCM sender ID and the Crashlytics
# mapping upload key; not a hard secret but Firebase recommends keeping
# it out of public source control.
app/google-services.json
